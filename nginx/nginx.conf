server {
    listen 80;  # Listen on HTTP port

    location /backups/ {
        root /usr/share/nginx/html;  # Serve files from /usr/share/nginx/html/backups/
        autoindex on;                # Show directory listing
    }
}
